<?php include('./includes/conn.php');

    $idt = $_POST['id'];

    $return = 0;

    echo "ss".$idt;
    #$mysqli = con_start();
    #$smtp = $mysqli->prepare("UPDATE  Tanda(id_user, name, intervalo_dias, num_personas, num_repeticiones, cantidad, fecha_inicial) VALUES (?,?,?,?,?,?,?) ");
?>
